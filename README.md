#This is just a simple compiler for a c program .
#to run it 
#make sure you have python installed in your machine
#clone the repository 
#unzip it and navigate to the directory
#run the runner.py and follow the prompt.
#NB: do not press the enter button until you have finished typing ,use the space bar for clean code
